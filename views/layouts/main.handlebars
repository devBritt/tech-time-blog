<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Time Blog</title>
    {{!-- Bootstrap framework --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {{!-- fonts --}}
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@500;600&family=Roboto&display=swap" rel="stylesheet">
    {{!-- icons --}}
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
    <link href="/stylesheets/styles.css" rel="stylesheet">
</head>
<body>
    {{!-- header start --}}
    <header class="navbar sticky-top" role="navigation">
        <div class="container-fluid">
            {{!-- title start --}}
            <a href="#" class="navbar-brand"><h1 class="title">Tech Time</h1></a>
            {{!-- title end --}}
            {{!-- nav start --}}
            <nav class="nav justify-content-end align-items-center">
                <a href="/" class="nav-link">home</a>
                {{#if loggedIn}}
                <a href="/dashboard" class="nav-link">dashboard</a>
                <a href="/" class="nav-link" id="logout">logout</a>
                {{else}}
                <a href="/login" class="nav-link">login</a>
                {{/if}}
            </nav>
            {{!-- nav end --}}
        </div>
    </header>
    {{!-- header end --}}
    {{!-- main start --}}
    <main class="container-fluid">
        {{{ body }}}
    </main>
    {{!-- main end --}}
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>