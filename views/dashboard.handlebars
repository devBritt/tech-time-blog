<section class="d-flex justify-content-center my-3">
    <h2 class="welcome">Welcome to your dashboard, <span class="username">{{username}}</span>!</h2>
</section>

<section class="row row-cols-1 row-cols-lg-2 mx-3">
    <div class="col create-new-post mb-3">
        <form id="new-post-form">
            <div class="mb-3">
                <label for="post-title">Title</label>
                <input type="text" name="post-title" id="post-title" class="form-control">
            </div>
            <div class="mb-3">
                <label for="post-content">Content</label>
                <textarea name="post-content" id="post-content" class="form-control" rows="15"></textarea>
            </div>                    
            <button type="submit" class="btn d-flex"><span class="material-icons">add</span>&nbsp;&nbsp;Create Post&nbsp;</button>
        </form>
    </div>
    <div class="col posts-list overflow-auto mb-3">
    {{#if posts}}
        {{#each posts as |post|}}
        <article class="card mb-3">
            <div class="card-body">
                <a href="/post/{{post.id}}" class="post-link"><h3 class="card-title">{{post.title}}</h3></a>
                <p class="card-subtitle">{{post.created_at}} by {{post.user.username}}</p>
                <p class="post-text">{{post.post_text}}</p>
                <a href="/post/{{post.id}}" class="card-link">
                {{post.comments.length}}
                comment(s)
                </a>
            </div>
        </article>
        {{/each}}
    {{else}}
    <div>
        <p class="subtitle">You haven't created any posts yet!</p>
    </div>
    {{/if}}
    </div>
</section>

<script src="/javascript/add-post.js"></script>