<section class="row-cols-1 row-cols-lg-2">
    {{#each posts as |post|}}
    <div class="col">
        <article class="card">
            <div class="card-body">
                <h3 class="card-title">{{post.title}}</h3>
                <p class="card-subtitle created-at">{{post.created_at}}</p>
                <p>{{post.post_text}}</p>
                <a href="/post/{{post.id}}" class="card-link">
                {{post.comments.length}}
                comment(s)
                </a>
            </div>
        </article>
    </div>
    {{/each}}
</section>